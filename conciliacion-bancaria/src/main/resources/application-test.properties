spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/conciliacion_test}
spring.datasource.username=${DB_USER:test}
spring.datasource.password=${DB_PASSWORD:test}
spring.flyway.baseline-on-migrate=true